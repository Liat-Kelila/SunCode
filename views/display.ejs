<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
      <link rel="stylesheet" href="style.css">
    <title></title>
  </head>
  <body>
        <h1>Song Collection</h1>
        <div class="container">
          <a href="/suncode" class="index-link" style="display: block;">Back Home</a><br>
            <!-- This is where the playlist gets generated and returned nicely! -->
            <%for(let i = 0; i < song.length; i++) {%>
                <ul class="list">
                  <li>Energy level: <%=song[i].energy%><br/>
                  <li><%=song[i].name%> by: <%=song[i].artist%></li>
                  <li>Listen here: <%=song[i].link%></li>
                  <div class="info-container">
                    <a href="/suncode/<%=song[i].id%>/edit">EDIT</a>
                      <form action="/suncode/<%=song[i].id%>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE">
                      </form>
                </ul>
              <% } %>
        </div>
    </body>
  </html>
